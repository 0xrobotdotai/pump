"use client";
import React from "react";
import { observer } from "mobx-react-lite";
import { useStore } from "@/store";


export const LayoutProvider: React.FC = observer(() => {
 

  return (
    <>
    </>
  );
});
