import CreateCoinContent from "@/components/create";

const Create: React.FC = () => {
  return (
    <main className="flex flex-row justify-evenly items-stretch">
      <CreateCoinContent />
    </main>
  );
};

export default Create;
